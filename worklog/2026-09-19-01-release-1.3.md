# Publish version 1.3

- Work date: 2026-09-19 (Asia/Bangkok)
- Type: current session
- Starting commit: `82cb4ec`; pending changes include the mixed-DPI wallpaper fix, regression test, agent notes, historical worklogs and rebuilt screen saver binary from prior tasks.

## Request
Push the completed changes to GitHub and publish release 1.3.

## Work performed
- Reviewed pending files, repository remote and existing v1.2 release assets.
- Set both application versions to 1.3.0 and added `RELEASE-NOTES-1.3.md` describing the DPI fix, downloads and validation limits.
- Committed source and documentation as `fc9a3fc`, then published both binaries from that committed source. All three assets report `1.3.0+fc9a3fcf22519f0a8699f3a9fdd6c1278bb21a1b`.
- Committed the rebuilt tracked screen saver as `b66703e`, created annotated tag `v1.3` at that commit, and atomically pushed main and the tag to origin.
- Published [GitHub Release 1.3](https://github.com/gimkim/scrapbook-screen-saver/releases/tag/v1.3) as the latest stable release, with `ScrapbookSaver.scr`, `ScrapbookSaver.exe` and `ScrapbookWallpaper.exe`. Existing releases retained.

## Verification
- Both Release win-x64 single-file publishes exited 0 without warnings/errors.
- Reran `dotnet run --project tests/WallpaperDpiRegression/WallpaperDpiRegression.csproj -c Release`: exit 0; initial geometry, three Settings closures and all four caller DPI contexts passed on two monitors. Local ignored report: `release-1.3-regression.txt`.
- `git diff --check` passed before publication.
- GitHub reports tag v1.3, non-draft and non-prerelease status, and three uploaded assets. Each remote asset's SHA256 digest matches its local release binary.
- The renderer smoke test passed in the preceding fix session; see `2026-09-18-03-wallpaper-mixed-dpi-rebuild.md`. No wallpaper process was running at the start of this release task; the regression exited without leaving an application instance running.

## Remaining work / limitations
Release publication is complete. Retain the documented UI-input and desktop validation limitations; no new claim of successful Save-button input. This completion record is committed separately after the release tag so publication facts can be recorded without rewriting the tagged source or binary provenance.
